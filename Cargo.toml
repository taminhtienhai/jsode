workspace = { members = ["jsode_macro"] }

[package]
name = "jsode"
version = "0.1.0"
edition = "2021"

[lints.clippy]
never_loop = "allow"

[dependencies]
jsode_macro = { path = "./jsode_macro" }

[features]
default = ["macro"]
macro = []
unstable = [] # introduce memory leak at the moment